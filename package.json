{
  "name": "croissant",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "client",
    "signer",
    "web"
  ],
  "scripts": {
    "dev": "bun run --cwd web dev",
    "build": "bun run --cwd client build && bun run --cwd web build",
    "build:client": "bun run --cwd client build",
    "lint": "eslint .",
    "preview": "bun run --cwd web preview"
  },
  "dependencies": {
    "lucide-react": "^0.488.0"
  }
}
