{
  "name": "croissant",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun build:client && bun build:web",
    "build:client": "wasm-pack build client --target web --out-dir client/pkg",
    "build:web": "bun --filter web build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "private": true,
  "workspaces": [
    "web",
    "client"
  ]
}
