{
  "name": "croissant",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun build:client && bun build:web",
    "build:client": "wasm-pack build client --target web --out-dir pkg",
    "build:web": "bun --filter web build:extension",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "private": true,
  "workspaces": [
    "web",
    "client"
  ],
  "dependencies": {
    "lucide-react": "^0.488.0"
  }
}
