{
  "name": "croissant",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "wasm-client",
    "web"
  ],
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm --filter wasm-client run build && pnpm --filter web run build:extension",
    "build:wasm-client": "pnpm --filter wasm-client run build",
    "build:web": "pnpm --filter web run build:extension",
    "lint": "eslint .",
    "preview": "pnpm run web preview"
  },
  "dependencies": {
    "lucide-react": "^0.488.0"
  }
}
